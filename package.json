{
  "name": "odoo-sh-deploy-test",
  "version": "1.0.0",
  "description": "React SPA for Odoo.sh deployment testing",
  "private": true,
  "homepage": "/deploy-test",
  "proxy": "http://localhost:8069",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "axios": "^1.6.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "build:odoo": "BUILD_PATH=./odoo_module/static/build react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [">0.2%", "not dead", "not op_mini all"],
    "development": ["last 1 chrome version", "last 1 firefox version", "last 1 safari version"]
  },
  "eslintConfig": {
    "extends": ["react-app"]
  }
}
